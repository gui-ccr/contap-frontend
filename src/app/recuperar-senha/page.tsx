import { RecoveryPage } from "@/features/auth/RecoveryPage";

// O Next.js precisa obrigatoriamente que a função seja exportada como default
// e que ela retorne o componente envelopado corretamente.
export default function RecuperarSenhaPage() {
  return <RecoveryPage />;
}